const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;const finePointer=matchMedia('(pointer:fine)').matches;const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:reduceMotion?'auto':'smooth',block:'start'})}}));const orbit=document.querySelector('.hero-orbit');if(orbit&&finePointer&&!reduceMotion){orbit.addEventListener('pointermove',e=>{const r=orbit.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;orbit.style.transform=`perspective(900px) rotateY(${x*7}deg) rotateX(${y*-7}deg)`});orbit.addEventListener('pointerleave',()=>orbit.style.transform='')}const glow=document.querySelector('.cursor-glow');if(glow&&finePointer&&!reduceMotion){let tx=innerWidth/2,ty=innerHeight/2,x=tx,y=ty;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY});(function loop(){x+=(tx-x)*.09;y+=(ty-y)*.09;glow.style.left=x+'px';glow.style.top=y+'px';requestAnimationFrame(loop)})()}document.querySelectorAll('.project').forEach(c=>c.addEventListener('pointermove',e=>{if(!finePointer)return;const r=c.getBoundingClientRect();c.style.setProperty('--mx',((e.clientX-r.left)/r.width)*100+'%');c.style.setProperty('--my',((e.clientY-r.top)/r.height)*100+'%')}));const copy=document.querySelector('.copy-link');copy?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(copy.dataset.copy);const old=copy.textContent;copy.textContent='Copied ✓';setTimeout(()=>copy.textContent=old,1600)}catch{copy.textContent='Copy unavailable'}});const theme=document.querySelector('.theme-toggle');const saved=localStorage.getItem('pijush-theme');if(saved==='light')document.documentElement.classList.add('light');const updateTheme=()=>{const light=document.documentElement.classList.toggle('light');localStorage.setItem('pijush-theme',light?'light':'dark');if(theme)theme.textContent=light?'☾':'☼'};if(theme){theme.textContent=document.documentElement.classList.contains('light')?'☾':'☼';theme.addEventListener('click',updateTheme)}const progress=document.querySelector('.scroll-progress');const updateProgress=()=>{if(!progress)return;const max=document.documentElement.scrollHeight-innerHeight;progress.style.transform=`scaleX(${max>0?scrollY/max:0})`};addEventListener('scroll',updateProgress,{passive:true});updateProgress();const run=document.querySelector('#run-code'),input=document.querySelector('#code-input'),output=document.querySelector('#code-output');run?.addEventListener('click',()=>{let logs=[];const safeLog=(...args)=>logs.push(args.map(v=>typeof v==='object'?JSON.stringify(v):String(v)).join(' '));try{const fn=new Function('console',input.value);fn({log:safeLog});output.textContent=logs.length?logs.join('\n'):'Code ran successfully — no output.'}catch(err){output.textContent='Error: '+err.message}});