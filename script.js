const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;const finePointer=matchMedia('(pointer:fine)').matches;const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:reduceMotion?'auto':'smooth',block:'start'})}}));const orbit=document.querySelector('.hero-orbit');if(orbit&&finePointer&&!reduceMotion){orbit.addEventListener('pointermove',e=>{const r=orbit.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;orbit.style.transform=`perspective(900px) rotateY(${x*7}deg) rotateX(${y*-7}deg)`});orbit.addEventListener('pointerleave',()=>orbit.style.transform='')}const glow=document.querySelector('.cursor-glow');if(glow&&finePointer&&!reduceMotion){let tx=innerWidth/2,ty=innerHeight/2,x=tx,y=ty;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY});(function loop(){x+=(tx-x)*.09;y+=(ty-y)*.09;glow.style.left=x+'px';glow.style.top=y+'px';requestAnimationFrame(loop)})()}document.querySelectorAll('.project').forEach(c=>c.addEventListener('pointermove',e=>{if(!finePointer)return;const r=c.getBoundingClientRect();c.style.setProperty('--mx',((e.clientX-r.left)/r.width)*100+'%');c.style.setProperty('--my',((e.clientY-r.top)/r.height)*100+'%')}));const overlay=document.querySelector('.command-overlay'),open=document.querySelector('.command-btn'),close=document.querySelector('.command-close');function toggle(){if(!overlay)return;const state=overlay.classList.toggle('open');overlay.setAttribute('aria-hidden',String(!state));document.body.style.overflow=state?'hidden':''}open?.addEventListener('click',toggle);close?.addEventListener('click',toggle);overlay?.addEventListener('click',e=>{if(e.target===overlay)toggle()});document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();toggle()}if(e.key==='Escape'&&overlay?.classList.contains('open'))toggle()});document.querySelectorAll('.command-menu a').forEach(a=>a.addEventListener('click',()=>{overlay?.classList.remove('open');overlay?.setAttribute('aria-hidden','true');document.body.style.overflow=''}));const copy=document.querySelector('.copy-link');copy?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(copy.dataset.copy);const old=copy.textContent;copy.textContent='Copied ✓';setTimeout(()=>copy.textContent=old,1600)}catch{copy.textContent='Copy unavailable'}});